# Web API framework
fastapi==0.115.0
# ASGI server
uvicorn[standard]==0.30.6
# Core LangChain APIs
langchain==0.3.7
# OpenAI-compatible chat/embedding client used with local model runner
langchain-openai==0.2.8
# PostgreSQL + pgvector integration for LangChain
langchain-postgres==0.0.12
# PostgreSQL driver
psycopg[binary]==3.2.3
# Environment settings loader
pydantic-settings==2.6.1
# PDF text extraction
pypdf==5.1.0
# DOCX text extraction
python-docx==1.1.2
