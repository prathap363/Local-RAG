server {
  # Expose static UI on container port 80.
  listen 80;
  server_name _;

  location / {
    # Serve frontend files and fallback to index for direct URL loads.
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri /index.html;
  }
}
